# Shell scripts are run by dpkg and rpm on the user's machine. Checked out with CRLF they fail as
# "/bin/bash^M: bad interpreter", which on rpm fails the whole transaction, so pin them to LF
# regardless of the committer's platform.
*.sh text eol=lf
